# Покроковий план завантаження на GitHub

Оскільки ви вже почали працювати через PowerShell і виконали `git init`, ось ваші наступні кроки:

## Крок 1: Підготовка файлів (локально)
Виконуйте ці команди одну за одною у вашому терміналі:

1. **Додайте всі файли до списку завантаження:**
   ```powershell
   git add .
   ```

2. **Збережіть зміни локально (коміт):**
   ```powershell
   git commit -m "First upload: restructured project"
   ```

3. **Створіть основну гілку:**
   ```powershell
   git branch -M main
   ```

---

## Крок 2: Зв'язок із сайтом GitHub
Тепер вам потрібно створити місце на самому GitHub, куди полетять файли.

1. Зайдіть на [github.com](https://github.com/) та створіть новий репозиторій (**New repository**). Назвіть його, наприклад, `vpoint-calculator`.
2. **Нічого не додавайте** (ні README, ні .gitignore) — просто натисніть **Create repository**.
3. Після створення ви побачите посилання. Воно виглядатиме приблизно так: `https://github.com/ВАШ_НІК/vpoint-calculator.git`. **Скопіюйте його.**

---

## Крок 3: Завантаження коду в інтернет
Поверніться в PowerShell і виконайте останні дві команди:

1. **Прив'яжіть вашу папку до копії в інтернеті:**
   *(Замініть посилання на те, яке ви скопіювали на кроці 2)*
   ```powershell
   git remote add origin https://github.com/ВАШ_НІК/vpoint-calculator.git
   ```

2. **Завантажте файли:**
   ```powershell
   git push -u origin main
   ```
   *GitHub може попросити вас авторизуватися у спливаючому вікні — просто підтвердіть вхід через браузер.*

---

## Крок 4: Публікація сайту (GitHub Pages)
Після того, як файли з'являться на сайті GitHub:
1. У вашому репозиторії на сайті клацніть **Settings** (Налаштування).
2. Зліва оберіть **Pages**.
3. У полі **Branch** оберіть гілку `main` та натисніть **Save**.
4. Зачекайте хвилину, і за посиланням, яке там з'явиться, ваш калькулятор буде працювати як сайт!

---

### Що робити при змінах у майбутньому?
Якщо ви зміните щось у коді і захочете оновити сайт, просто введіть:
```powershell
git add .
git commit -m "Опис змін (наприклад: змінив логотип)"
git push
```

## 💡 Поради
- **Файл .gitignore**: Рекомендується створити файл `.gitignore` у корені проекту та додати туди рядки, які не треба вантажити на GitHub (наприклад, `*.crswap` або тимчасові файли).
- **Оновлення**: Щоразу, коли ви вносите зміни, використовуйте:
  ```powershell
  git add .
  git commit -m "Опис змін"
  git push
  ```
